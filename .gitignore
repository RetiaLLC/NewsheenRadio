# build output
.pio/
dist/*.bin

# The catalogue IS committed: the "with catalog" release variant merges it into
# a factory image, and CI builds from a clean checkout, so it has to be in the
# repo. Refresh it with tools/radio_garden_dump.py + build_catalogue.py; expect
# a large diff when you do.
tools/radio_garden_out/
tools/*.log

# Personal audio dropped in for local playback
data/*.mp3

# macOS
.DS_Store

# platform-generated
src/idf_component.yml*
